// scripts/theme-toggle.js
const toggleTheme = () => {
  document.body.classList.toggle('light');
};

// Optional: Add a button somewhere to trigger it
// e.g., <button onclick="toggleTheme()">Toggle Theme</button>